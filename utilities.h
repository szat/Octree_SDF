#pragma once

#include <vector>
#include <iostream>
#include <string>

std::vector<double> rayTriangleIntersect(const std::vector<double> & source, const std::vector<double> & dir, const std::vector<std::vector<double>> & tri) {
	//This function returns a coordinate in 3d 
	
	//Validate data

	//A vector of points in 3d
	double epsilon = 0.0000001;
	
}

bool rayInBox(const std::vector<double> & source, const std::vector<double> & dir, const std::vector<std::vector<double>> & box) {
	//This function returns a boolean is the ray is in the box 

	//Validate data

	//A vector of points in 3d
	double epsilon = 0.0000001;

}